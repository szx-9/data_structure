# data structure note
 ## lesson 1
 
1字符数组：反斜杠标识，

2 atoi标准库 array to integer
（1利用遍历指针而非下标 2跳过前导零 3处理正负号 4逐位判断游标是否是数字
然后进行读取 5return）

3 gets：一次读取一行，结尾加入反斜杠0

4 FILE：
fopen（return指针，在内存中的首地址）：对于cpu是对内存数据进行读取，
		如果当前.c与文件在同一个目录中，否则加入绝对路径（从根目录cd等
		开始进行绝对路径输入，包括扩展名）
		w模式下，是只写（只能调用写函数），会直接清空！！！记得备份，
		调用时，malloc调用一个FILE结构体内存，堆上
		FILE只在内存中保存操作状态（系统描述符），读写缓冲区，读写位置指针


fgetc（FILE*）：获取文件指针的字符

fputc（char ,FILE*);
读写功能中自动进行移动！！

fgets(s,n,in):
	从文件in（stream传入stdin）
	中读取一行，最多n-1个字符（\0)，放入s数组中
	换行符不会处理，直接输入然后将\0也一起放入数组中


fputs(s,out):
	把字符串s写入文件out中

fscanf(in,"%d",&score)
	从文件in中读取一个整数

fprintf(in,"d\n",out);

5文件名通常不超过32，屏幕一行通常80，1024通常为物理行长度最大值

6字符串查找n*m朴素算法

7define：（绕过编译，节省时间）
	常量
	宏函数（可以定义变元，AB自动识别）：参数间不能有空格且要用括号括起来！！
 
8二维数组
	数组嵌套用花括号，或者用有序序列，
	没有初始化的部分直接初始化为零，

9算法实例（二维数组地图染色）
	对于某个陆地，只有边缘进行了表示，求岛的所有面积
	朴素算法：对某点进行四个方向遍历，若遇到四个边界则加入（但对于凹进去的形状不行）
	对边界四条边进行bfs遍历，遇到岛屿停止

10指针
	函数指针（指向一个函数）：int* funcp（）括号表示函数指针
	野指针：初始化的时候必须置空，否则是野指针
	指针本身是变量，占用内存（但指针的值为一个地址，也需要指向某个
	系统内存


